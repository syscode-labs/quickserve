version: 2

project_name: quickserve

before:
  hooks:
    - go mod tidy

builds:
  - id: quickserve
    main: .
    binary: quickserve
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      - -s -w -X github.com/syscode-labs/quickserve/internal/app.Version={{ .Tag }} -X github.com/syscode-labs/quickserve/internal/app.Commit={{ .Commit }} -X github.com/syscode-labs/quickserve/internal/app.Date={{ .Date }}

archives:
  - id: default
    ids:
      - quickserve
    formats:
      - tar.gz
    name_template: "{{ .ProjectName }}_{{ .Tag }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats:
          - zip
    files:
      - README.md
      - LICENSE
      - CHANGELOG.md

checksum:
  name_template: checksums.txt

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"

release:
  header: |
    ## quickserve {{ .Tag }}

    Warning: `quickserve` has no authentication. If `-upnp` or `-tunnel cloudflare` succeeds, served files may be reachable from the public internet.
  footer: |
    Verify downloads with `checksums.txt` and GitHub artifact attestations.
